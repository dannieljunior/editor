unit untEngeEnumerados;

interface

type
  TTipoAutenticacao = (taLauncher, taDebug);
  TTipoConexao = (tcFirebird = 0, tcMSSQL = 1, tcOracle = 2);
  TSistema = (siProtesto, siRtd, siNtcs, siRics, siCivil);
  TTipoDriver = (tdNativo = 0, tdDevart = 1);
implementation

end.



